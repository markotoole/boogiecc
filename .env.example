# Sanity CMS Configuration
# Get these values from your Sanity project dashboard at https://sanity.io/manage

# Your Sanity project ID (required)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Your Sanity dataset (usually 'production' or 'development')
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API token for write operations (get from Sanity dashboard -> API tokens)
# Only needed if you want to write data from your app (e.g., comments, likes)
SANITY_API_TOKEN=your_api_token_here
